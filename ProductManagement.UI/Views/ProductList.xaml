<UserControl x:Class="ProductManagement.UI.Views.ProductList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ProductManagement.UI.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"></ColumnDefinition>
            <ColumnDefinition Width="200"></ColumnDefinition>
            <ColumnDefinition Width="100"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <Label Grid.Row="0" Grid.Column="0"
               HorizontalAlignment="Right"
               VerticalAlignment="Center"
               Content="Category:"></Label>
        <ComboBox Grid.Row="0" Grid.Column="1"  
               ItemsSource="{Binding Categories}"
              SelectedValue="{Binding SelectedCategoryId, Mode=TwoWay}"
              SelectedValuePath="Id"
              DisplayMemberPath="Name"
               HorizontalAlignment="Stretch"
                  Margin="10,0,0,0"
               VerticalAlignment="Center"></ComboBox>
        <Label Grid.Row="0" Grid.Column="2"
           HorizontalAlignment="Right"
           VerticalAlignment="Center"
           Content="SKU:"></Label>
        <TextBox Grid.Row="0" Width="120" VerticalAlignment="Center" HorizontalAlignment="Left" Grid.Column="3" ></TextBox>
        <StackPanel Margin="10,5,0,0" HorizontalAlignment="Left" Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="3" Orientation="Horizontal">
            <Button VerticalAlignment="Center" HorizontalAlignment="Left" Content="Search" Width="80"></Button>
            <Button Margin="10,0,0,0" VerticalAlignment="Center" HorizontalAlignment="Left" Content="Clear" Width="80"></Button>
        </StackPanel>
    </Grid>
</UserControl>
